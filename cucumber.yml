# Reports
reports: -p html_report -p json_report
html_report: --format html -o reports/features_report.html
json_report: --format json -o reports/features_report.json

# Brands e Environments
magluiza_prd: HOST='https://www.magazineluiza.com.br/'
magluiza_hlg: HOST='https://www.magazineluiza-hlg.com.br/'

## Execution device
desktop: DEVICE=desktop

# Graphic mode
headless: GRAPHIC_MODE=headless
graphic:  GRAPHIC_MODE=graphic

# Execution
local:  MODE=local

# Template
default: -p pretty
pretty: --format pretty
